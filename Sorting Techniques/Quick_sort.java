class Quick_sort{
    public static void main(String args[])
    {
        int arr[]={3,7,1,5,10,2};

        Quick(arr,0,arr.length-1);
    }
    public static void Quick(int arr[],int low,int high) {
        int low=0;
        int high=arr.length-1;
        if(low<high)
        {
            int pivot=Quick(arr,low,)
            Quick(arr,low,high);

            partation(arr,low,piovot-1)
        }
    }
}