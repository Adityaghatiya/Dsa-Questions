public clIFY THE ARGUMENTS WITH "final" PREFIX. IT IS READ ONLY     public int[] slidingMaximum(final int[] A, int B) {         int len=A.length;         int i=0;         int j=0;         int arr[]=new int[len-B+1];         int s=0;         int ans=0;         Deque<Integer>q=new LinkedList<>();         while(j<len)         {             if(q.size()>0)             {                 while(q.size()>0 && q.getLast()<A[j])                 {                     q.removeLast();                 }                 q.addLast(A[j]);             }             else{                 q.addFirst(A[j]);             }                          if(j-i+1<B)             {                 j++;             }             else if(j-i+1==B)             {                 arr[s]=q.getFirst();                 s++;                 if(A[i]==q.getFirst())                 {                    q.removeFirst();                  }                 i++;                 j++;             }         }         return arr;     
